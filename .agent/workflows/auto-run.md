---
description: Workflow for sambung-kata project with auto-run enabled
---

// turbo-all

Steps for common operations:

1. Stage all changes: `git add -A`
2. Commit with message: `git commit -m "message"`
3. Push to GitHub: `git push origin master`
4. Check file line count: `(Get-Content file | Measure-Object -Line).Lines`
5. Check git status: `git status`
